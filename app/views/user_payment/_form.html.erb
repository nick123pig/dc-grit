<%= form_for(user_payment) do |f| %>
  <% if user_payment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user_payment.errors.count, "error") %> prohibited this User Payment from being saved:</h2>

      <ul>
      <% user_payment.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

    <div class="form-group">
      <label class="sr-only" for="amount">Amount (in dollars)</label>
      <div class="input-group">
        <div class="input-group-addon">$</div>
        <input type="text" class="form-control" id="amount" name="amount" placeholder="Amount">
      </div>
    </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>


